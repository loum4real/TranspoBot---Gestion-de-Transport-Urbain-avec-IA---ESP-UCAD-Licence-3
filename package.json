{
  "name": "transpobot",
  "version": "1.0.0",
  "description": "TranspoBot Assistant IA Transport (FastAPI + Ollama)",
  "scripts": {
    "dev": "python -m uvicorn app:app --reload --host 0.0.0.0 --port 8000",
    "start": "python -m uvicorn app:app --host 0.0.0.0 --port 8000",
    "install-deps": "pip install -r requirements.txt"
  },
  "keywords": ["transpobot", "fastapi", "transport", "ia"],
  "author": "GLSi L3 — ESP/UCAD"
}
